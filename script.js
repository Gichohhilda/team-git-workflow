document.addEventListener("DOMContentLoaded", () => {
    const actionBtn = document.getElementById("action-btn");

    actionBtn.addEventListener("click", () => {
        // update the test
        alert("Button clicked! This is part of my Git practice.");
    });
});